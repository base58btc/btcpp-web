<!DOCTYPE html>
<html>
  <head>
    <title>speaker applicants @ bitcoin++</title>
    {{ block "header" . }} {{ end }}
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>&#127908;</text></svg>">
  </head>
  <body>
    {{ block "mainnav" . }} {{ end }}
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="mx-auto max-w-5xl">
            <section id="topline" class="mb-6">
                <div class="overflow-hidden sm:rounded-lg bg-white shadow">
                    <div class="px-4 py-5 sm:p-6">
                        <h1 class="text-xl font-bold text-gray-900">Speaker Applicants</h1>
                        <p class="text-sm text-gray-500">{{ .Conf.Desc }} - {{ .Conf.DateDesc }}</p>
                    </div>
                </div>
            </section>

            {{ if .FlashMessage }}
            <section class="mb-6">
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
                    {{ .FlashMessage }}
                </div>
            </section>
            {{ end }}

            {{ if .Rows }}
            <form method="POST" action="/admin/applicants/{{ .Conf.Tag }}/email">
            <section id="applicants-table" class="mb-6">
                <div class="bg-white shadow rounded-lg overflow-hidden">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-3 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-10">
                                    <input type="checkbox" onclick="toggleAll(this)" class="rounded border-gray-300">
                                </th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Talk</th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            {{ range .Rows }}
                            <tr class="hover:bg-gray-50">
                                <td class="px-3 py-4">
                                    <input type="checkbox" name="proposal_refs" value="{{ .Proposal.ID }}" class="app-checkbox rounded border-gray-300">
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-900">{{ if .Speaker }}{{ .Speaker.Name }}{{ end }}</td>
                                <td class="px-6 py-4 text-sm text-gray-900">{{ if .Speaker }}{{ .Speaker.Email }}{{ end }}</td>
                                <td class="px-6 py-4 text-sm text-gray-500">{{ .Proposal.Title }}</td>
                                <td class="px-6 py-4 text-sm text-gray-500">{{ .Proposal.Status }}</td>
                                <td class="px-6 py-4 text-right">
                                    {{ if or (eq .Proposal.Status "Accepted") (eq .Proposal.Status "TheyDecline") (eq .Proposal.Status "WeDecline") (eq .Proposal.Status "Rejected") }}
                                    <span class="text-xs text-gray-400">—</span>
                                    {{ else }}
                                    <span class="inline-flex items-center gap-2">
                                        {{ if ne .Proposal.Status "Invited" }}
                                        <button type="submit"
                                            formaction="/admin/conf/{{ $.Conf.Tag }}/review/{{ .Proposal.ID }}/invite?return=%2Fadmin%2Fapplicants%2F{{ $.Conf.Tag }}"
                                            formmethod="POST"
                                            class="inline-flex items-center rounded-md bg-green-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-green-700">
                                            Invite
                                        </button>
                                        {{ end }}
                                        {{ if and (ne .Proposal.Status "Waitlisted") (ne .Proposal.Status "Invited") }}
                                        <button type="submit"
                                            formaction="/admin/conf/{{ $.Conf.Tag }}/review/{{ .Proposal.ID }}/waitlist?return=%2Fadmin%2Fapplicants%2F{{ $.Conf.Tag }}"
                                            formmethod="POST"
                                            class="inline-flex items-center rounded-md bg-yellow-500 px-3 py-1.5 text-xs font-semibold text-white hover:bg-yellow-600">
                                            Waitlist
                                        </button>
                                        {{ end }}
                                        <button type="submit"
                                            formaction="/admin/conf/{{ $.Conf.Tag }}/review/{{ .Proposal.ID }}/decline?return=%2Fadmin%2Fapplicants%2F{{ $.Conf.Tag }}"
                                            formmethod="POST"
                                            class="inline-flex items-center rounded-md bg-gray-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-gray-700">
                                            Decline
                                        </button>
                                        <button type="submit"
                                            formaction="/admin/conf/{{ $.Conf.Tag }}/review/{{ .Proposal.ID }}/confirm?return=%2Fadmin%2Fapplicants%2F{{ $.Conf.Tag }}"
                                            formmethod="POST"
                                            class="text-xs text-gray-500 hover:text-gray-800 underline bg-transparent border-0 p-0">
                                            Mark Confirmed
                                        </button>
                                    </span>
                                    {{ end }}
                                </td>
                            </tr>
                            {{ end }}
                        </tbody>
                    </table>
                    <div class="px-6 py-3 bg-gray-50 text-sm text-gray-500">
                        {{ len .Rows }} applicants
                    </div>
                </div>
            </section>

            {{ template "email_compose" .EmailCompose }}
            </form>
            {{ else }}
            <section class="mb-6">
                <div class="bg-white shadow rounded-lg px-4 py-5 sm:p-6 text-center text-gray-500">
                    No applicants found for this event.
                </div>
            </section>
            {{ end }}
        </div>
    </div>
    <script>
    function toggleAll(source) {
        var checkboxes = document.querySelectorAll('.app-checkbox');
        checkboxes.forEach(function(cb) { cb.checked = source.checked; });
    }
    </script>
  </body>
</html>
